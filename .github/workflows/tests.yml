name: OrbiFlow CI

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop, main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create temporary .env file
        run: |
          echo "DB_HOST=db" >> .env
          echo "DB_NAME=orbiflow_db" >> .env
          echo "DB_USER=admin" >> .env
          echo "DB_PASS=admin_pass" >> .env
          echo "POSTGRES_DB=orbiflow_db" >> .env
          echo "POSTGRES_USER=admin" >> .env
          echo "POSTGRES_PASSWORD=admin_pass" >> .env
          echo "POSTGRES_HOST=db" >> .env
          echo "POSTGRES_PORT=5432" >> .env
          echo "DJANGO_SECRET_KEY=ci_test_key_placeholder" >> .env
          echo "DJANGO_DEBUG=true" >> .env

      - name: Build and Run Tests
        run: |
          # Primero construimos para asegurar que todo esté listo
          docker compose build
          # Corremos los tests usando el Makefile
          make tests