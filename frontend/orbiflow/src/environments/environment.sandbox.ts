// Usado por Vercel al buildear con --configuration=sandbox (default actual).
// Apunta al backend de Render conectado a la rama develop + Neon Sandbox.
export const environment = {
  production: true,
  apiUrl: 'https://orbiflow-backend-sandbox.onrender.com/api'
};
